Code style preferences:
- No section header comments (no "# ---- UTILS ----" blocks)
- No obvious inline comments explaining what a line does
- Comments only for non-obvious "why", never "what"
- Prefer clear variable names over explanatory comments
- No TODO/FIXME placeholders left in code

API: This project uses the Groq API via the openai-compatible client (groq package).